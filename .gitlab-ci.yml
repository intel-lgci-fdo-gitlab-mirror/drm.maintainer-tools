image: fedora:latest

before_script:
  - dnf install -y ShellCheck python3-docutils python3-sphinx make

check:
    stage: build
    script:
      - make check

pages:
    stage: deploy
    script:
      - make html
      - cp -r _build/html public
    artifacts:
        paths:
          - public
    only:
        refs:
          - master
